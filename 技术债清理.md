研究完成。以下是技术债清理的详细实施计划：

Plan: Phase 1 技术债清理
TL;DR — 7 个独立子任务，涉及后端 ~15 个文件、前端 ~8 个文件的修改。主要工作：移除废弃的邀请码模块（影响面最广，涉及所有 service test 的 mock 初始化）、统一 handler context helper、拆分大组件、清理未使用依赖、修复 index.html、统一 API 响应格式。排班警告和密码重置经核实已正确实现，无需修复。

Steps

1. 移除废弃的邀请码模块（影响面最大）
后端删除/修改 ~12 个文件：

router.go — 删除 L37(POST /register)、L39(GET /invite/:code)、L51(POST /invite) 三条路由
auth_handler.go — 删除 Register(L160-175)、ValidateInvite(L138-156)、GenerateInvite(L112-132) 三个方法，删除 handleAuthError 中 ErrInviteCodeInvalid 映射(L229)
auth_service.go — 从 AuthService 接口删除 Register、GenerateInvite、ValidateInvite 三个方法签名；删除实现(L128-256, L377-436)；删除 generateInviteCode 辅助函数(L497-507)；删除 ErrInviteCodeInvalid 错误变量(L31)
auth.go — 删除 RegisterRequest(L13-21) 和 GenerateInviteRequest(L28-30) DTO
删除整个文件 invite_code_repo.go
删除整个文件 invite_code.go
repository.go — 从 Repository 结构体删除 InviteCode 字段(L14)、从 NewRepository 删除初始化(L35)、从 WithTx 删除(L76)
auth_service_test.go — 删除 mockInviteCodeRepo 定义(L236-270)、newMockInviteCodeRepo()、以及 9 个相关测试函数(TestRegister_*, TestGenerateInvite_*, TestValidateInvite_*)
mock_repos_test.go — 删除 mockInviteCodeRepo 及 newMockInviteCodeRepo()
其余 10 个 *_service_test.go 文件 — 从 newTestRepo() 辅助函数中删除 InviteCode: newMockInviteCodeRepo() 这一行
可选：从 init.sql 的 L869-906 删除 CREATE TABLE invite_codes DDL（或加注释说明已废弃）
2. 统一 Handler Context Helper
修改 2 个文件：

time_slot_handler.go — 将 3 处 callerID, _ := c.Get("user_id") (L68, L94, L114) 替换为 callerID := MustGetUserID(c)，这些位置当前忽略了 exists 返回值，未认证时会 panic
auth_handler.go — 将 L168 和 L191 的 userID, exists := c.Get("user_id") 替换为 MustGetUserID(c)（MustGetUserID 内部已处理不存在的情况，定义在 context_helper.go）
3. 拆分大组件文件
前端修改 ~10 个文件：

ConfigPage 拆分（ConfigPage.tsx 824行 → 6 个文件）：

新建 frontend/src/pages/admin/config/SemesterTab.tsx（提取 L62-295）
新建 frontend/src/pages/admin/config/TimeSlotTab.tsx（提取 L300-363）
新建 frontend/src/pages/admin/config/LocationTab.tsx（提取 L367-561）
新建 frontend/src/pages/admin/config/ScheduleRuleTab.tsx（提取 L565-660）
新建 frontend/src/pages/admin/config/SystemParamTab.tsx（提取 L664-771）
ConfigPage.tsx 缩减为 ~50 行，import 5 个 Tab 组件
DashboardPage 拆分（DashboardPage.tsx 456行 → 4 个文件）：

新建 frontend/src/pages/dashboard/AdminDashboard.tsx（提取 L43-216）
新建 frontend/src/pages/dashboard/LeaderDashboard.tsx（提取 L219-319）
新建 frontend/src/pages/dashboard/MemberDashboard.tsx（提取 L321-429）
DashboardPage.tsx 缩减为 ~25 行
4. 清理未使用依赖
修改 package.json：

移除 dependencies: @fullcalendar/core, @fullcalendar/daygrid, @fullcalendar/interaction, @fullcalendar/react, @fullcalendar/timegrid（5 个包，0 处导入）
移除 devDependencies: msw（0 处导入）
保留 tailwindcss + @tailwindcss/vite（虽然当前未使用，但已配置好，后续 Phase 2 页面开发可能启用；如确认不用则也移除）
运行 npm install 更新 lockfile
5. 修正 index.html 标题
修改 index.html L8：<title>Vite + React + TS</title> → <title>Echo Union - 值班管理系统</title>

6. 统一 API 响应格式
前端共 10 处 Array.isArray(raw) ? raw : raw.list 防御性代码，分布在 4 个文件中：

方案 A（推荐）：在 services/api.ts 的 Axios 响应拦截器中统一适配——如果 response.data.data 是含 list 属性的对象，自动展开为数组。然后删除 10 处重复的防御代码

方案 B：排查后端所有列表接口，统一返回格式（要么全部返回数组，要么全部返回 { list: [], total: N }），前端删除防御代码

7. 排班警告 和 密码重置 — 无需修改
经核实：

AutoSchedule 已有完善的警告机制：返回 Warnings []string + TotalSlots/FilledSlots 计数
ResetPassword 已使用 crypto/rand 生成 8 位随机密码，非可预测的 "Ec"+学号后6位
Verification

后端：运行 go build [Project](http://_vscodecontentref_/24). 确认编译通过，运行 go test [Project](http://_vscodecontentref_/25). 确认所有测试通过（移除邀请码后测试数量减少 ~9 个是预期行为）
前端：运行 npm run build 确认构建通过，运行 npm run test 确认测试通过
手动验证：启动开发环境，测试登录 → 各管理页面正常加载 → 排班功能正常
Decisions

邀请码模块完全移除（而非仅标记 deprecated），因文档已明确废弃且改为批量导入
API 响应格式统一选方案 A（前端拦截器适配），不修改后端，避免影响已运行的其他客户端
Tailwind CSS 暂保留，为 Phase 2 预留；FullCalendar 和 msw 移除
init.sql 中邀请码表 DDL 加 -- DEPRECATED 注释保留，不删除，避免影响已有数据库